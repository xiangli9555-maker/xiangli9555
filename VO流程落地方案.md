# VO 整体流程 · 落地方案（PM 规格 2026-07-22）

> 来源：用户梳理的 6 步 VO 全流程 + 1 个额外情况（新选角）。
> 目标：把每条流程落到对应页面（需求汇总 / 台词管理 / 声优库 / 录制档期），并明确字段、交互、数据持久化方式。
> 视觉统一：v13.2 色板（荧光青绿 `#0FF796` + 深蓝黑 `#192229`），组件规格不变。

---

## Step 1 · AI 拉取 TAPD → 过滤 VO → 汇总页 + 同步字段
- **落地位置**：需求汇总页 + 后端 `dfai-live-server.js`（已建）
- **逻辑**：`get_story` → 过滤 `type=音频` & `status≠suspended` & 仅 VO（剔除 BGM/音效）→ 拆单合并 → 34 行
- **同步字段**：发布计划 / Area / 标题 / 创建人 / 开发人员 / 状态 / 处理人 + 台词-中 owner→文案策划 + `tapd_url`(父需求长 ID)
- **现状**：快照模式 34 行；顶栏已有「从 TAPD 刷新」按钮（打 `/api/refresh` 强制重拉）。**读**半边已活。
- **待办**：给 `DFAI_API_TOKEN` 切到实时拉取（developer 字段才填充）。

## Step 2 · 自动建在线表格（按策划汇总）
- **落地位置**：需求汇总页「台词表」列（已显示该策划汇总文档链接 + ⚡生成并发送）+ 接 `tencent-docs-oa` MCP（技能 `vo-lines-smartsheet`）
- **规则**：每策划 1 份在线表格；其名下每需求 1 个 tab（9 列）；**表格名 = 【策划名】+【发布计划】+【需求数】+【DDL】**；每发布计划每策划整理一次即可。
- **待办**：真跑 MCP 前需文案策划名单 + 各自负责需求（建文档时必需）；游戏角色 99 项核对。

## Step 3 · 澄清会 → 粗估详情（核心待落地）
- **澄清会**：文案策划找创单人澄清 → 每需求所需声优 + 大概录制句数。
- **落地 A · 需求汇总页「澄清/粗估」面板**：
  - 每行 demand 可填：预估 CN 声优数 / EN 声优数 / 预估句数 / 预估录制时长(h) / 澄清状态(待澄清·已澄清) / 澄清会日期。
  - 顶部按**发布计划聚合**：需约 CN 声优 X / EN 声优 Y / 预估句数 Z / 预估时长 Hh → 直接支撑「约声优 + 谈档期 + 判时长」。
  - 持久化：localStorage（按 demand id），**不污染** `TAPD_SNAPSHOT`。
- **落地 B · 音频策划填录制档期怎么方便**（Step 3 子问题 → 录制档期页）：
  - 提供「按需求批量建档期」向导：选发布计划 → 拉该计划全部需求 → 每行自动带出预估句数/声优 → 音频策划只填 棚/日期/时段 → 自动算时长。避免手填。

## Step 4 · 交接 & 偏差（待落地）
- 澄清后 2 周交接真实台词表：汇总各需求声优 + 真实句数。
- **规则**：每 20 字/词 = 1 句；对比 Step 3 预估 → 偏差 → 加场/删场录制。
- **落地**：台词管理页「交接」动作导入/粘贴真实句数 → 自动比对预估 → 标红偏差>阈值的需求 → 提示加/删场。

## Step 5 · 锁表 & 导出发包（待落地）
- 录制前 1 周锁在线表格（禁编辑）；>1 周仍可改台词。
- **发包窗口**：按声优汇总该版本同声优台词汇总 → 下载发包。
- **落地**：台词管理页「锁表」按钮（置只读 + toast）；「导出·按声优」→ 选版本+声优 → 生成 xlsx（接后端 exceljs，按 `vo-lines-smartsheet` 既有导出逻辑横切）。

## Step 6 · 新选角（额外情况）✅ 本次已实现
- 需求澄清周开选角会；新声优由文案策划 1 周内上传选角信息：
  - **A** 基本信息（年龄/性别/口音）· **B** 人设&背景故事 · **C** 试音台词(≥5句) · **D** 参考音视频链接
- **声优库落地**（已做）：
  - 加「选角信息」列：doc 图标缩略图 → 点开详情弹窗（A/B/C/D + Word 文档链接 + **存档上传记录**）。
  - 收到文档后音频策划 2 周内选声优录入 → 声优库加「试音」列：上传最终选定声优的声音（音频播放器 + 上传）。
  - 数据 localStorage 持久化；「+ 新增声优」弹窗录入核心字段；行首状态灯（绿完整/黄部分/灰空缺）。

---

## 数据持久化策略（原型期）
| 页面 | 持久化 | 说明 |
|---|---|---|
| 需求汇总 | `TAPD_SNAPSHOT`（真实）+ localStorage（澄清/粗估补充字段） | 不污染快照 |
| 声优库 | localStorage `vo_roster_v1` | 选角/试音元数据持久；音频 blob 仅会话内可播 |
| 台词管理 | 待接 MCP / 后端 DB | Step 4/5 时建 |
| 录制档期 | 待建 | Step 3B 向导时建 |

## 下一步优先级建议
1. **Step 3A 需求汇总「澄清/粗估」面板**（PM 最强调，直接产出粗估详情 + 按发布计划聚合）
2. **Step 3B 录制档期「批量建档期」向导**（接 Step 3A 的预估句数）
3. **Step 4 交接偏差比对**（接 Step 3A 预估 + 台词真实句数）
4. **Step 5 锁表 + 导出发包**
