# === Vo Manager 环境变量 · 复制到 .env 使用 ===

# MySQL root 密码（首次启动生效，之后改无效；请设强密码）
DB_PASSWORD=vo_manager_pwd_2026

# DFAI TAPD 同步 Token（可选，暂不填留空即可）
DFAI_TOKEN=

# 上传音频文件的最大体积（Nginx 层）
MAX_UPLOAD_SIZE=100M
