# 台词表 · 纯腾讯在线表格方案（智能表格 SmartSheet）

> 决策（2026-07-22）：放弃"本地 xlsx 上传解析"，改为**纯腾讯在线表格**闭环。
> 因用户非企业微信管理员，走**个人腾讯文档 + 官方 MCP**（无需管理员、无需自建 OAuth 后端）。

---

## 一、为什么是这条路

| 方案 | 谁能配 | 复杂度 | 结论 |
|---|---|---|---|
| 企业微信文档 | 必须企业超级管理员 | 中 | ❌ 用户非管理员，卡死 |
| 本地 xlsx 上传解析 | 用户自己 | 低 | ⚠️ 不是"在线表格"，已被本次否决 |
| 个人腾讯文档 + OAuth 后端 | 用户自己 | 高（回调域名+授权流程） | ⚠️ 可行但重 |
| **个人腾讯文档 + 官方 MCP** | **用户自己（个人 Token）** | **低** | ✅ **采用** |

腾讯文档官方提供 MCP 服务（`tencent-docs`），带完整 `smartsheet.*` 工具集。
配一个个人 Token 即可，助手（我）能直接建表、读表、汇总、导出。

---

## 二、三层结构（与之前锁定的模型一致）

- **第 1 层（录入）**：每个文案策划 = 1 份腾讯智能表格文档；文档内**每个子表(tab) = 他名下的一条需求**。文案策划在对应 tab 里填台词。
- **第 2 层（汇总）**：同一份文档天然就是该策划的汇总视图（多 tab）。PM 让助手"同步" → 读所有 tab → 汇入台词管理页。
- **第 3 层（导出）**：按声优横切，导出单页发录音棚。

> 智能表格 = 1 文档 + 多子表，天然把"每需求独立"与"每策划汇总"合一，不用维护两份。

---

## 三、台词表 schema · v4（2026-07-23 用户定稿）

> 决策：**方式 B**——澄清元信息内联在每个需求 tab 顶部（不单起 tab）。理由：一份文档已按策划汇总，元信息跟需求走。

### 3.1 每个需求 tab 的行布局

| 行号 | 作用 | 内容 |
|------|------|------|
| 1 | 元信息表头 | `需求澄清梗概` \| `游戏角色名` \| `句数预估` |
| 2 | 元信息值 | 由 PM/文案在澄清会后填 |
| 3 | 空行 | — |
| 4 | 空行 | — |
| 5 | 台词表头 | 见 3.2 |
| 6+ | 台词逐条 | 文案填 |

### 3.2 台词区字段（10 列）

| # | 字段名 | 类型 | 说明 | 谁填 |
|---|--------|------|------|------|
| 1 | NO.序号 | 自动编号/数字 | 行号 | 系统 |
| 2 | 游戏角色名 | **单选 · 下拉锁定** | 选项来自声优表（TBD，用户整理中），后续由工具自动同步为下拉选项 | 文案选 |
| 3 | 台词-中 | 文本（多行） | 中文台词 | 文案填 |
| 4 | 台词-英 Lines | 文本（多行） | 英文台词 | 文案填 |
| 5 | 情绪 | **单选(14)** | 平静/紧张/兴奋/愤怒/悲伤/恐惧/嘲讽/惊讶/疲惫/命令/低语/怒吼/喜悦/痛苦 | 文案选 |
| 6 | 触发条件 | 文本 | 何时播放 | 文案填 |
| 7 | GP Audio Event/音频文件名 | 文本 | 音频事件名或文件名 | 文案填 |
| 8 | 备注信息 | 文本 | 补充说明 | 文案填 |
| 9 | 录制时间-中 | 日期/日期时间 | 中配档期定档后**由工具按 `游戏角色名→中配声优→档期` 自动回填** | 工具（PM 可覆盖） |
| 10 | 录制时间-英 | 日期/日期时间 | 英配档期定档后**由工具按 `游戏角色名→英配声优→档期` 自动回填** | 工具（PM 可覆盖） |

### 3.3 相较 v3 的变更

- ➕ 顶部新增 3 列元信息区（澄清梗概/角色名/句数预估）+ 2 空行分隔
- ➕ 末尾新增 2 列「录制时间-中」「录制时间-英」（VO 6 步流程 · Step 3B 档期定档后由工具回填 → Step 5 锁表导出前必备）
- ➖ 移除「模块」列（不再要求文案选模块，靠角色单选即可关联）
- ➖ 移除「中配声优」「英配声优」两列（角色单选后，声优信息由工具在**汇总视图 / 导出发包**时按角色→声优映射自动回填，不再进文案填写区）
- 🔄 「序号」→「NO.序号」；「台词-英」→「台词-英 Lines」；「GP Audio Event」→「GP Audio Event/音频文件名」；「备注」→「备注信息」

### 3.3.1 录制时间回填流程（Step 3B → 5）

```
① 澄清会 → 得到 游戏角色名 + 句数预估（已填元信息 & 各条台词行的角色）
② 约演员 → 声优库定档 → 录制档期页 落档期表（角色↔声优↔日期）
③ PM 触发「同步录制时间到台词表」：
     for each 台词行:
       cn_va = voice_roles_map[角色].cn_va
       en_va = voice_roles_map[角色].en_va
       录制时间-中 ← schedule[cn_va].date   (未定档留空、有变更覆盖，幂等)
       录制时间-英 ← schedule[en_va].date
④ 录制前 1 周锁表 + 按声优汇总导出 xlsx（Step 5）
```

### 3.4 锁死机制

- **表头锁死**：智能表格的"字段(列)"就是表头，PM 通过 MCP 建好，文案只能新增/编辑**记录(行)**，改不动列结构。
- **角色单选锁死**：`游戏角色名` 用单选字段，选项从**网站声优库**同步；文案只能下拉选、不能手填。
- **情绪单选锁死**：14 个选项写死。

### 3.5 声优库 · 权威数据源（2026-07-23 落地）

- **来源**：`assets/声优库.xlsx`（用户从企微导出的初始快照，99 角色 · 6 分类：指挥官7/干员19/Boss16/AI兵12/NPC42/AI系统音3）
- **产物**：`assets/roster.json` + `assets/roster.js`（自动生成，字段：id/category/role_cn/role_en/gender/cn_va/cn_location/cn_studio/en_va/en_location/en_studio）
- **网站入口**：`preview-声优库-精修版.html` 加载 roster；顶部编制分布动态生成；表格 10 列真实数据可搜索、可按分类筛选、可行内编辑（编辑差异存 `localStorage vo_roster_edits_v1`，不覆盖原始）
- **消费方**：
  - 台词表「游戏角色名」下拉选项 = `roster.role_cn` 全集
  - 台词表末 2 列「录制时间-中/英」= `schedule[roster.role_cn→cn_va/en_va].date`
  - 录制档期页批量建档期 = roster 声优清单 + 语种（cn_va/en_va 拆分）

---

## 四、闭环流程（授权后由助手执行）

```
① PM 让助手："给文案A建台词文档" 
   → 我用 MCP 建 1 份智能表格文档，按 A 名下需求逐个建子表 + 建 11 字段（含下拉选项）
   → 返回文档链接，PM 发给文案 A

② 文案 A 在腾讯文档里逐条填台词（模块/角色/情绪从下拉选，台词中英手填）

③ PM 让助手："同步文案A的台词"
   → 我用 MCP 读该文档所有子表记录
   → 按角色从声优库回填中/英配声优
   → 写入后端 script_lines（幂等，按 需求+序号 判重）
   → 台词管理页实时看到汇总

④ PM 让助手："导出 山新 的台词" 
   → 后端按声优筛选生成单页 xlsx → 直接发录音棚
```

后端只做**持久化 + 导出**（`script_lines` 表、`/api/lines`、`/api/lines/export`，上回合已建好）；
腾讯在线表格的建/读全部走 MCP，由助手在对话里驱动。

---

## 五、启用步骤（需要你做，一次性）

1. 打开 **https://docs.qq.com/open/auth/mcp.html** ，用你的微信/QQ 登录，复制个人 **Token**。
2. 把 Token 填进 `C:\Users\lycheelli\.workbuddy\mcp.json` 里 `Authorization` 那一行（替换占位文字），**或直接把 Token 发我，我帮你填**。
3. 到连接器管理页右上角"自定义连接器"，对新出现的 `tencent-docs` 点**"信任"**启用。
4. 告诉我"已启用" → 我就开始建第一份文案策划的台词文档，跑通建表→填→同步→导出全链路。

## ⚠️ 一个必须先知道的成本点

腾讯文档 MCP 的智能表格能力**可能需要腾讯文档 VIP**（API 返回 `400007 VIP权限不足` 时即需升级）。
普通读写文档不一定要，但 smartsheet 高级操作大概率要。建议先用 Token 建一份测试文档验证；若报 400007，再决定是否开 VIP。

---

## 六、休眠代码（保留不删）

- `deploy/backend/src/wecom.js` / `linesheet.js`：企业微信智能表格版，等你日后拿到企微管理员权限可激活。
- `deploy/backend/src/upload.js`：本地 xlsx 上传解析版，作为无网络/离线兜底保留。
- `assets/台词表模板_v3.xlsx`：离线母版，现降级为"字段结构参考 + 离线兜底填写"。
