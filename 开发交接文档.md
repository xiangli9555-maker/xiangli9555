# 游戏语音需求管理系统 — 开发交接文档

> **文档版本**：v1.0 · 2026-07-17
> **产品负责人**：lycheelli (PM)
> **协作方**：文案策划 · 音频策划 · 后端工程师 · 前端工程师
> **原型参考**：`./互动原型-语音需求管理系统.html`

---

## 目录

1. [产品概述](#1-产品概述)
2. [数据模型](#2-数据模型)
3. [核心功能规格](#3-核心功能规格)
4. [第三方集成](#4-第三方集成)
5. [权限控制矩阵](#5-权限控制矩阵)
6. [API 路由约定](#6-api-路由约定)
7. [前端组件与技术栈建议](#7-前端组件与技术栈建议)
8. [部署与环境](#8-部署与环境)
9. [验收标准与红线](#9-验收标准与红线)
10. [里程碑](#10-里程碑)

---

## 1. 产品概述

一站式游戏语音需求管理系统，覆盖「需求录入 → 台词编写 → 声优匹配 → 录制排期 → 音频归档」全流程。

**五模块闭环**：

```
需求汇总 → 台词管理 → 声优库 → 录制档期 → VO 资产库
   ↑___________________________________________|
              （音频反哺需求验收）
```

**目标用户**：
- PM（产品经理）：统筹版本节点、跟踪需求进度
- 文案策划：填写文案台词（中/英）
- 音频策划：分配声优、上传录制版台词与音频
- 管理员：维护声优库、系统权限

---

## 2. 数据模型

### 2.1 `demands`（需求表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 需求 ID |
| version | VARCHAR(32) | NOT NULL, INDEX | 版本号（Yang1..Yang5） |
| area | ENUM('AI','SOL','MP') | NOT NULL, INDEX | 业务区域 |
| task_name | VARCHAR(128) | NOT NULL | 任务名 |
| description | TEXT | | 需求概括 |
| video_sync | TINYINT(1) | DEFAULT 0 | 是否音画同步 (0/1) |
| req_planner | VARCHAR(32) | NOT NULL | 需求策划 |
| script_planner | VARCHAR(32) | | 文案策划 |
| en_writer | VARCHAR(32) | | 英文文案 |
| status | ENUM('pending','progress','done','sync') | DEFAULT 'pending' | 状态 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | |
| updated_at | DATETIME | ON UPDATE CURRENT_TIMESTAMP | |

**索引**：
- `idx_ver_area (version, area)`
- `idx_status (status)`

---

### 2.2 `script_lines`（台词表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 台词 ID |
| demand_id | BIGINT | FK → demands.id, INDEX | 关联需求 |
| no | VARCHAR(16) | NOT NULL | 序号（如 001） |
| voice_actor_id | BIGINT | FK → voice_actors.id, NOT NULL | 声优 ID（枚举，禁止自由文本） |
| text_cn | TEXT | | 文案台词-中 |
| text_en | TEXT | | 文案台词-英 |
| recorded_text_cn | TEXT | | 录制版台词-中 |
| recorded_text_en | TEXT | | 录制版台词-英 |
| trigger_condition | VARCHAR(64) | | 触发条件 |
| emotion | VARCHAR(32) | | 语言情绪 |
| gp_audio_event | VARCHAR(64) | INDEX | GP Audio Event 事件码 |
| remark | VARCHAR(255) | | 备注信息 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | |
| updated_at | DATETIME | ON UPDATE CURRENT_TIMESTAMP | |

**索引**：
- `idx_demand (demand_id)`
- `idx_va (voice_actor_id)`
- `idx_event (gp_audio_event)`

---

### 2.3 `voice_actors`（声优库）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 声优 ID |
| name | VARCHAR(64) | NOT NULL, UNIQUE | 姓名（唯一） |
| role_type | ENUM('干员','指挥官','路人角色') | NOT NULL, INDEX | 类型 |
| languages | VARCHAR(64) | NOT NULL | 语种（中文/英文/中英双语） |
| current_schedule | VARCHAR(64) | | 当前档期（区间字符串） |
| available | TINYINT(1) | DEFAULT 1 | 是否可接新角色 |
| portfolio_url | VARCHAR(255) | NOT NULL | 选角资料 URL（**必填**） |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | |
| updated_at | DATETIME | ON UPDATE CURRENT_TIMESTAMP | |

**约束**：
- `portfolio_url` 必填，业务层校验：新增/更新时必须上传选角资料文件（PDF/JPG/PNG），后端存 COS 后回填 URL

---

### 2.4 `audio_assets`（资产库）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 资产 ID |
| script_line_id | BIGINT | FK → script_lines.id, NOT NULL | 关联台词行 |
| language | ENUM('cn','en') | NOT NULL, INDEX | 语言 |
| file_url | VARCHAR(255) | NOT NULL | COS/OSS 文件 URL |
| file_name | VARCHAR(128) | NOT NULL | 原始文件名 |
| duration | DECIMAL(6,2) | | 时长（秒） |
| file_size | BIGINT | | 大小（字节） |
| version | VARCHAR(32) | INDEX | 冗余记录（便于全局检索） |
| gp_audio_event | VARCHAR(64) | INDEX | 冗余记录 |
| voice_actor_id | BIGINT | FK → voice_actors.id, INDEX | 冗余记录 |
| uploaded_by | VARCHAR(32) | NOT NULL | 上传人 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP, INDEX | |

**说明**：
- 冗余 `version` / `gp_audio_event` / `voice_actor_id` 是为了 VO 资产库全局检索时避免多表 JOIN

---

### 2.5 `script_line_history`（台词变更留痕表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | |
| script_line_id | BIGINT | NOT NULL, INDEX | 关联台词 |
| field_name | VARCHAR(32) | NOT NULL | 变更字段 |
| old_value | TEXT | | 旧内容 |
| new_value | TEXT | | 新内容 |
| modified_by | VARCHAR(32) | NOT NULL | 修改人 |
| modified_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 修改时间 |

**触发时机**：
- 通过后端 API 拦截，在更新 `script_lines` 前将 diff 写入本表
- 必须记录字段：`text_cn` / `text_en` / `recorded_text_cn` / `recorded_text_en` / `voice_actor_id`

---

### 2.6 `recording_schedules`（录制档期表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | |
| record_date | DATE | NOT NULL, INDEX | 录制日期 |
| voice_actor_id | BIGINT | FK, NOT NULL | 声优 |
| language | ENUM('cn','en') | NOT NULL | 语种 |
| script_line_id | BIGINT | FK | 关联台词（可为空） |
| gp_audio_event | VARCHAR(64) | | GP Event（冗余） |
| duration | DECIMAL(4,1) | | 时长（小时） |
| status | ENUM('pending','progress','done') | DEFAULT 'pending' | |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | |

---

## 3. 核心功能规格

### 3.1 侧边栏树形导航

**结构**：

```
📋 需求汇总（可展开）
   ├─ Yang1
   ├─ Yang2
   ├─ Yang3
   ├─ Yang4
   └─ Yang5
📝 台词管理（可展开）
   ├─ AI
   ├─ SOL
   └─ MP
🎤 声优库（可展开）
   ├─ 干员
   ├─ 指挥官
   └─ 路人角色
📅 录制档期（无子集）
🎧 VO 资产库（无子集）
```

**交互**：
- 点击一级菜单 → 展开/收起子集，chevron 图标 90 度旋转
- 点击子集 → 高亮 active，主内容区加载对应数据
- 无子集的菜单 → 点击直接进入页面
- 状态管理：URL hash 或 React Router 记录当前 tab（便于分享 / 刷新还原）

---

### 3.2 需求汇总页

- 顶部 filter：AREA / 状态 / 策划人（策划人选项从当前版本的 tasks 动态生成）+ 搜索框 + 重置
- 表格列：任务名 / AREA / 需求概括 / 音画同步（黄色高亮）/ 需求策划 / 文案策划 / 状态 / 操作
- 点击行的「查看台词 →」 → 跳转到台词管理页对应的 AREA
- 表格加载：分页（20 条/页），大数据量用虚拟滚动
- 新建任务：模态框，字段=完整 `demands` 表

---

### 3.3 台词管理页

- URL：`/scripts/:area`（area ∈ AI / SOL / MP）
- 10 列固定表头（严禁增删列）
- **声优列**：`<select>` 组件，options 来自 `GET /api/voice-actors?type=all`，禁止 `<input type="text">`
- 可 inline 编辑的列：
  - 文案台词-中/英 → 权限：`role=script_writer` 或 `admin`
  - 录制版台词-中/英 → 权限：`role=audio_planner` 或 `admin`
  - 其他列（触发条件/情绪/GP Event/备注）→ `role=admin`
- 单元格 blur 时触发保存，PATCH `/api/script-lines/:id`
- 保存前后端写入 `script_line_history`
- 底部实时统计：`每 20 词 = 1 句`（中文按字符计，英文按空格分词）

---

### 3.4 声优库

- URL：`/voice-actors/:type`（type ∈ 干员 / 指挥官 / 路人角色）
- 表格列：姓名 / 语种 / 类型 / 当前档期 / 可接新角色（toggle） / 选角资料 / 操作
- 新增声优（弹窗）：
  - 姓名（必填）
  - 语种（下拉：中文/英文/中英双语）
  - 类型（下拉：干员/指挥官/路人角色）
  - **选角资料文件（必填，PDF/JPG/PNG）** — 未上传即禁止提交
  - 提交流程：先上传文件到 COS 拿 URL → 再 POST `/api/voice-actors`
- Toggle「可接新角色」：PATCH `/api/voice-actors/:id { available }`
- 删除：只有 admin 可见 `DELETE` 按钮

---

### 3.5 录制档期（日历视图）

- URL：`/calendar`
- **必须使用可视化日历组件**（推荐：Ant Design `<Calendar>` 或 FullCalendar 或 react-big-calendar）
- 月视图：显示当月每一天
- 每日格子展示：
  - 数字（日期）
  - 彩色圆点：中文=蓝(#3B82F6) / 英文=红(#EF4444) / 中+英=紫(#8B5CF6)
  - 事件名前 2 条 + `+N 更多` 提示
- 点击某日 → 右侧详情面板 or 弹窗（Modal）展示当日所有排期
- 支持：上/下月切换、跳转到今天、按声优/语言筛选
- 数据源：`GET /api/schedules?year=YYYY&month=MM&voice_actor_id=&language=`

---

### 3.6 VO 资产库

- URL：`/assets`
- 顶部 filter：声优 / GP Event / 版本 / 文件名搜索 + 重置
- 表格列（含全选复选框）：文件名 / 声优 / GP Event / 版本 / 时长 / 大小 / 上传时间 / 操作(试听 / 下载)
- 表头复选框 → 全选当前筛选结果
- 顶部按钮「批量下载 ZIP (N)」：POST `/api/assets/batch-download`，后端流式打包 ZIP，返回 blob
- 单个下载：GET `/api/assets/:id/download`（302 到 COS 签名 URL）
- 试听：`<audio>` 播放签名 URL

---

## 4. 第三方集成

### 4.1 腾讯文档 API（可选）

- **用途**：将 TAPD 已收集的需求同步到 `demands` 表，减少手工录入
- OAuth2.0 授权，读取指定表格
- 定时同步（每 30 分钟）或触发式同步（前端「同步」按钮）
- 表头字段与 `demands` 表字段的映射写在 `config/tencent_doc_mapping.yaml`

### 4.2 COS / OSS

- **腾讯云 COS**（推荐）或阿里云 OSS
- 分 3 个 bucket：
  - `voice-portfolio`（声优选角资料）
  - `voice-assets`（录制音频）
  - `voice-temp`（临时上传中转）
- 上传：前端拿后端签名的临时凭证，直传 COS，成功后回调后端登记
- 下载：后端生成 15 分钟签名 URL

### 4.3 通知（可选）

- 企业微信机器人 or 邮件（SMTP）
- 触发时机：
  - 新排期创建 → 通知声优 + 音频策划
  - 排期状态变更 → 通知需求策划
  - 音频上传完成 → 通知文案策划核对

---

## 5. 权限控制矩阵

| 操作 | 文案策划 | 音频策划 | 管理员 |
|------|:---:|:---:|:---:|
| 查看需求汇总 | ✅ | ✅ | ✅ |
| 新建/编辑需求 | ❌ | ❌ | ✅ |
| 查看台词表 | ✅ | ✅ | ✅ |
| 编辑「文案台词」列 | ✅ | ❌ | ✅ |
| 编辑「录制版台词」列 | ❌ | ✅ | ✅ |
| 修改声优下拉 | ❌ | ✅ | ✅ |
| 查看声优库 | ✅ | ✅ | ✅ |
| 新增/删除声优 | ❌ | ❌ | ✅ |
| Toggle 声优档期开关 | ❌ | ✅ | ✅ |
| 查看录制档期 | ✅ | ✅ | ✅ |
| 新增/修改档期 | ❌ | ✅ | ✅ |
| 查看 VO 资产库 | ✅ | ✅ | ✅ |
| 上传音频 | ❌ | ✅ | ✅ |
| 批量下载 | ✅ | ✅ | ✅ |
| 删除资产 | ❌ | ❌ | ✅ |

**实现建议**：
- 后端：中间件 `requireRole(['script_writer', 'admin'])`
- 前端：`<PermissionGate role="...">` 组件包裹敏感按钮
- 用户角色存 JWT，服务端 double check

---

## 6. API 路由约定

### 6.1 基本约定

- Base URL：`/api/v1`
- 认证：`Authorization: Bearer <JWT>`
- 响应：`{ code: 0, data: ..., message: '' }`

### 6.2 端点清单

**需求（demands）**：
```
GET    /api/v1/demands?version=&area=&status=&planner=&keyword=&page=&size=
GET    /api/v1/demands/:id
POST   /api/v1/demands
PATCH  /api/v1/demands/:id
DELETE /api/v1/demands/:id            (admin only)
```

**台词（script_lines）**：
```
GET    /api/v1/script-lines?demand_id= or ?area=
GET    /api/v1/script-lines/:id
POST   /api/v1/script-lines
PATCH  /api/v1/script-lines/:id       (触发 script_line_history 留痕)
DELETE /api/v1/script-lines/:id       (admin only)
GET    /api/v1/script-lines/:id/history
```

**声优（voice_actors）**：
```
GET    /api/v1/voice-actors?role_type=&available=
GET    /api/v1/voice-actors/:id
POST   /api/v1/voice-actors           (portfolio_url 必填)
PATCH  /api/v1/voice-actors/:id
DELETE /api/v1/voice-actors/:id       (admin only)
```

**排期（recording_schedules）**：
```
GET    /api/v1/schedules?year=&month=&voice_actor_id=&language=
POST   /api/v1/schedules
PATCH  /api/v1/schedules/:id
DELETE /api/v1/schedules/:id
```

**资产（audio_assets）**：
```
GET    /api/v1/assets?voice_actor_id=&gp_audio_event=&version=&keyword=
POST   /api/v1/assets                 (script_line_id + file 二段上传)
GET    /api/v1/assets/:id/download    (302 → COS 签名 URL)
POST   /api/v1/assets/batch-download  (body: { asset_ids: [] })
DELETE /api/v1/assets/:id             (admin only)
```

**COS 签名**：
```
POST   /api/v1/cos/signature          (body: { bucket, key })
```

---

## 7. 前端组件与技术栈建议

| 类别 | 推荐 | 备注 |
|------|------|------|
| 框架 | React 18 + TypeScript | Vue 3 亦可 |
| 组件库 | Ant Design 5 | Table / Form / Select / Modal / Calendar 全部到位 |
| 状态管理 | Zustand or Redux Toolkit | 小规模项目 Zustand 更轻 |
| 路由 | React Router v6 | 支持嵌套路由匹配侧边栏子集 |
| 请求 | Axios + React Query | React Query 处理缓存/重试 |
| 日历 | Ant Design `<Calendar>` 或 `@fullcalendar/react` | 后者更强大，前者集成简单 |
| 表格 | Ant Design `<Table>` 或 TanStack Table | 大数据量用后者 + 虚拟滚动 |
| 文件上传 | Ant Design `<Upload>` + COS SDK | |
| 打包 | Vite | HMR 快 |

---

## 8. 部署与环境

**环境**：
- Dev：本地开发，Mock 数据
- Staging：内网测试，接真实 COS
- Prod：生产，域名 `voice.example.com`

**部署方式**：
- 前端：静态站点，Nginx 或 CDN
- 后端：Node.js（NestJS）or Golang（Gin）
- 数据库：MySQL 8.0
- 缓存：Redis（会话 + 声优列表缓存）

**环境变量**：
```
DATABASE_URL=mysql://...
REDIS_URL=redis://...
COS_SECRET_ID=...
COS_SECRET_KEY=...
COS_BUCKET_PORTFOLIO=...
COS_BUCKET_ASSETS=...
JWT_SECRET=...
TENCENT_DOC_APPID=...        (可选)
TENCENT_DOC_APPSECRET=...    (可选)
WECOM_WEBHOOK=...            (可选)
```

---

## 9. 验收标准与红线

### 🚨 红线（必须满足）

1. **声优名称必须枚举化** — 台词管理页的声优列**只能是 `<select>`**，禁止 `<input>`
2. **修改台词必须留痕** — 每次 PATCH `/api/script-lines` 必须写入 `script_line_history`
3. **日历视图必须可视化** — 不允许用列表替代，必须有月/日格子布局
4. **新增声优必须上传选角资料** — 后端校验 `portfolio_url` 非空
5. **权限硬约束** — 前端隐藏 + 后端拒绝，双重防线

### 验收 Checklist

- [ ] 五模块闭环全部可用
- [ ] 侧边栏树形导航可展开/收起，子集切换正确
- [ ] 需求汇总按 Yang1..Yang5 正确过滤
- [ ] 台词表格声优列为下拉框，选项来自声优库
- [ ] 文案/音频角色只能编辑对应列（前端 disabled + 后端 403）
- [ ] 台词编辑历史可查
- [ ] 声优新增无选角资料时被拦截
- [ ] 声优 toggle 开关实时保存
- [ ] 日历显示当月所有排期，圆点颜色正确
- [ ] 日历点击某日展示详情
- [ ] 日历支持月份切换 + 声优/语言筛选
- [ ] VO 资产库全局检索 + 批量下载 ZIP
- [ ] 音频试听 / 单个下载可用
- [ ] 单元测试覆盖率 ≥ 60%

---

## 10. 里程碑

| 阶段 | 内容 | 预估工时 | 交付物 |
|------|------|:---:|------|
| M1 | 数据库建表 + Mock 数据 + API 骨架 | 3d | DB Schema + Swagger |
| M2 | 权限系统 + 登录 + JWT | 2d | 登录页 + 权限中间件 |
| M3 | 需求汇总 + 台词管理页 | 5d | 需求 / 台词两个页面 |
| M4 | 声优库 + 文件上传 | 3d | 声优管理 + COS 集成 |
| M5 | 录制档期日历 | 3d | 日历视图 + 排期 CRUD |
| M6 | VO 资产库 + 批量下载 | 3d | 检索 + ZIP 打包 |
| M7 | 腾讯文档同步 + 通知 | 3d | 定时任务 + 企微机器人 |
| M8 | 联调 + 测试 + 上线 | 4d | 生产环境部署 |
| **合计** | | **~26d** | |

---

## 附录：字段映射（前端 ↔ 后端）

| 前端字段 | 后端字段 | 说明 |
|---|---|---|
| `ver` | `version` | 版本 |
| `area` | `area` | AI/SOL/MP |
| `name` | `task_name` | 任务名 |
| `summary` | `description` | 需求概括 |
| `sync` | `video_sync` | 音画同步 |
| `planner` | `req_planner` | 需求策划 |
| `writer` | `script_planner` | 文案策划 |
| `va` | `voice_actor.name` | 声优（联表） |
| `zh` / `en` | `text_cn` / `text_en` | 文案台词 |
| `zh_rec` / `en_rec` | `recorded_text_cn` / `recorded_text_en` | 录制版 |
| `trigger` | `trigger_condition` | 触发条件 |
| `mood` | `emotion` | 情绪 |
| `event` | `gp_audio_event` | GP Event |
| `note` | `remark` | 备注 |
| `link` | `portfolio_url` | 选角资料 |

---

**文档结束。有任何疑问 @lycheelli**
